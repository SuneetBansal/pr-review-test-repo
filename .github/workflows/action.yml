name: 'pr-review'
on: [pull_request]
jobs:
  job1:
    name: 'code-review'
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pull-requests: write
    steps:
      - name: Code Review
        uses: SuneetBansal/ai-driven-pr-review@main
        with:
          openai-key: ${{ secrets.OPENAI_API_KEY }}
          prompt: 'Please review the Javascript code from Performance point of view, provide your feedbacks.'
